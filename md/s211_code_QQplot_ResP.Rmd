```{r}
fm_s211_pbc <- gls(log(serBilir) ~ year + year:drug + year * sex + age, data = pbc2,
                   correlation = corCAR1(form = ~ year | id))

qqnorm(fm_s211_pbc, ~ resid(., type = "p"))
```

